# Docker Compose Environment Variables
# SECURITY: This file contains EXAMPLE values only
# Copy to .env and replace with actual secure values
# NEVER commit the actual .env file to version control!

# ============================================================================
# DATABASE CREDENTIALS (REQUIRED)
# ============================================================================

# MongoDB Credentials
# Generate: openssl rand -base64 24
MONGODB_ROOT_USERNAME=admin_user
MONGODB_ROOT_PASSWORD=CHANGE_ME_secure_password_min_16_chars
MONGODB_DB_NAME=company_kb
MONGODB_PORT=27017

# Qdrant API Key
# Generate: openssl rand -hex 32
QDRANT_API_KEY=CHANGE_ME_min_32_character_api_key_required
QDRANT_PORT=6333
QDRANT_GRPC_PORT=6334

# ArangoDB Root Password
# Generate: openssl rand -base64 24
ARANGO_ROOT_PASSWORD=CHANGE_ME_secure_password_min_16_chars
ARANGODB_PORT=8529

# Redis Password (optional but recommended)
# Generate: openssl rand -base64 24
REDIS_PASSWORD=CHANGE_ME_secure_password_min_16_chars
REDIS_PORT=6379

# ============================================================================
# SECURITY WARNING
# ============================================================================
# 
# Before deploying to production:
# 1. Replace ALL "CHANGE_ME" values with actual secure credentials
# 2. Verify .env is in .gitignore
# 3. Never share credentials via chat, email, or screenshots
# 4. Use different credentials for dev/staging/production
# 5. Rotate credentials every 90 days
#
# Generate secure passwords:
#   openssl rand -base64 32    # For passwords
#   openssl rand -hex 32       # For API keys
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#
# ============================================================================
